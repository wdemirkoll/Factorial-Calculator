# 🧮 Factorial Calculator

This project is a simple C++ program that takes an integer from the user and calculates the factorial of that number.

## 💡 Features
- Receives a number input from the user.
- Warns for negative numbers (because factorial is not defined for negative numbers).
- Calculates the factorial for positive numbers and prints it on the screen.

## 🧠 What is Factorial?
The factorial of a number is the product of all positive numbers from 1 to that number.

Example: 5! = 1 × 2 × 3 × 4 × 5 = 120

### Compilation:
```bash
g++ main.cpp -o randomname.exe
